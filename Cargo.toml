[package]
name = "licenserc"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4", features = ["derive"] }
schemars = "0.8"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
serde-saphyr = "0.0.2"

[dev-dependencies]
glob = "0.3"
